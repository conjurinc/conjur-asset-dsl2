# Given a host without an id, it should raise an error message
---
conjur:
  ---
  []

policy:
  - !host
    annotations:
      description: I should have an id, right?

exception:
  type: RuntimeError
  message: missing id for