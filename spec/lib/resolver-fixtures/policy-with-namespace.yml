# Namespace is applied to the policy, and subsequently to the policy body.
policy: |
  - !policy
    id: myapp
    body:
    - !host the-host

namespace: dev

expectation: |
  ---
  - !policy
    id: dev/myapp
  - !host
    id: dev/myapp/the-host
