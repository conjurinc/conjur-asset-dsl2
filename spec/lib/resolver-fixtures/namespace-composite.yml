# Namespace prefixes are applied to objects which are nested within top-level
# records, such as role and member within a role grant.
policy: |
  - !grant
    role: !group admin
    member: !user /alice

namespace: dev
  
expectation: |
  ---
  - !grant
    member: !member
      role: !user
        id: alice
    role: !group
      id: dev/admin
